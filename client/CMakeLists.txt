include_directories(include)
set(src src/client.cpp src/request.cpp src/response.cpp)
add_library(httpclient SHARED ${src})
target_compile_options(httpclient PUBLIC -O2 -Wall -Weffc++ -pedantic -Wextra)
target_link_libraries(httpclient pthread)