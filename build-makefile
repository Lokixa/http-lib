
all: build
	clear
	./${executable}

mem: build
	clear
	valgrind --leak-check=full -s ./${executable}

build: clean
	ninja

clean:
	ninja -t cleandead

prod: build
	strip ${executable}

executable = main
